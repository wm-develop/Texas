# 阶段 1：规则与协议清单

目标：完成服务端权威规则引擎、WebSocket 协议 v1 和自动牌局模拟器；连续模拟十万手，无筹码守恒错误和无法结束的牌局。

## 规则规格和协议

- [x] 输出《德州扑克规则规格 v1》。
- [x] 输出《WebSocket 协议 v1》。
- [x] 协议外壳加入 `tableRevision`。
- [x] 为协议消息建立 Go 类型和 JSON 合约测试。
- [x] 实现请求幂等、序号和快照恢复骨架。
- [x] 完成聊天消息权限、长度和频率模型。
- [x] 建立并测试 TRTC UserSig 的会话用户与牌桌语音权限授权链（阶段 2 接入真实账号和房间仓储）。

## 牌和牌型

- [x] 定义点数、花色和牌的合法性。
- [x] 实现五至七张牌的最佳五张牌计算。
- [x] 覆盖高牌到同花顺的全部标准牌型。
- [x] 覆盖踢脚比较和 `A-2-3-4-5` 顺子。
- [x] 覆盖公共牌即最佳牌型和平局。
- [x] 拒绝重复牌和无效输入。
- [x] 实现可注入随机源的 52 张牌牌堆和洗牌。

## 底池和结算

- [x] 实现主池和多层边池切分。
- [x] 弃牌玩家投入计入金额但排除获奖资格。
- [x] 退回无人跟注的超额下注。
- [x] 每个边池独立牌型比较和分配。
- [x] 平局分池和庄家左侧顺时针余数分配。
- [x] 底池构建筹码守恒测试。
- [x] 结算生成筹码守恒的账本事件，并提供拒绝重复写入的追加式存储骨架。

## 牌桌状态机

- [x] 实现 2～10 人座位、庄家按钮和盲注规则。
- [x] 实现翻牌前、翻牌、转牌和河牌下注轮。
- [x] 实现让牌、跟注、下注、加注、弃牌和全下。
- [x] 实现小额全下不重新开放加注权。
- [x] 实现超时自动让牌或弃牌。
- [x] 实现一手中断线和结算后离桌。
- [x] 实现 `handId`、`actionId` 和 `tableRevision` 校验。

## 自动模拟验收

- [x] 实现无界面虚拟玩家。
- [x] 随机模拟 2～10 人合法动作。
- [x] 每手检查筹码、底池和牌的唯一性。
- [x] 每手设置最大 500 次动作，检测无法结束的牌局。
- [x] 连续模拟十万手。
- [x] 记录随机种子，失败时可复现。

## 十万手验收结果

> 验证日期：2026-08-24

- 固定种子：`20260824`。
- 完成手数：`100000`。
- 执行玩家动作：`1762656`。
- 单手最大动作数：`84`，未触发 500 次保护上限。
- 筹码守恒、非负筹码、牌唯一性、合法动作和必然结束检查全部通过。
- `go test ./...` 与 `go vet ./...` 全部通过。

## 阶段退出结论

阶段 1 退出标准已满足。规则引擎和协议骨架可进入阶段 2 集成；账号、房间、持久化数据库和真实会话仓储属于阶段 2 工作。

## 当前验证命令

```powershell
cd C:\Programming\Texas\services\game_server
& 'C:\Programming\Texas\.toolchains\go\bin\go.exe' test ./...
& 'C:\Programming\Texas\.toolchains\go\bin\go.exe' vet ./...
```
